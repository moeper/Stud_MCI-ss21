<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kundenapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KundenApp">
        <activity
            android:name=".PrivacyActivity2"
            android:label="title_activity_privacy2"/>
        <!--android:theme="@style/Theme.KundenAppNoActionBar" >-->
    <activity
        android:name=".MainActivity"
        android:label="title_activity_main"/>

    <activity
        android:name=".LoadscreenActivity"
        android:label="@string/app_name">
        <!--android:theme="@style/Theme.KundenAppNoActionBar" >-->
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
</application>

</manifest>